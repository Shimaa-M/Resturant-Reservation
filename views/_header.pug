header.header
  nav.nav.nav--tours
    a.nav__el(href='/') Spectra
  nav.nav.nav--user
    if user
      a.nav__el(href='/reserve') Reserve Now
      a.nav__el.nav__el--logout Log out
      span.nav__user-img.heading-secondary= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up